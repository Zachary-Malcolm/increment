# Checks every push and pull request: types, unit tests, and that every course answer is correct.
name: CI

on:
  push:
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npm run typecheck
      - run: npm test
      # Runs every solution, starter, prediction and puzzle through real Python (Pyodide).
      - run: npm run check-content
